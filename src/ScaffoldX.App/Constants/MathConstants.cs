namespace ScaffoldX.App.Constants;

/// <summary>
/// Mathematical constants used across the annotation system.
/// </summary>
public static class MathConstants
{
    /// <summary>Conversion factor from radians to degrees (180 / PI).</summary>
    public const double RadiansToDegrees = 180.0 / Math.PI;

    /// <summary>Conversion factor from degrees to radians (PI / 180).</summary>
    public const double DegreesToRadians = Math.PI / 180.0;
}
