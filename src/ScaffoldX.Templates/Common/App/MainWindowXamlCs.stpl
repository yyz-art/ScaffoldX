##OUTPUT: src/{{ProjectName}}.App/MainWindow{{XamlCodeBehindExt}}
using System.Windows;

namespace {{ NamespacePrefix }}.App;

/// <summary>
/// 主窗口。
/// </summary>
public partial class MainWindow : Window
{
    public MainWindow()
    {
        InitializeComponent();
    }
}
